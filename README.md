# steam_free_game (AstrBot 插件)
\n+自动抓取 Steam 促销信息，发现 **限时免费（-100%）** 的游戏时，向配置的白名单群聊/私聊发送通知。\n+\n+## 功能\n+\n+- 自动抓取 Steam 商店搜索接口（`store.steampowered.com/search/results/`）的促销条目\n+- 过滤出 `-100%` 且现价为 `Free/0` 的条目\n+- 推送内容包含：游戏名称、游戏图片、原价、购买链接\n+- 使用 `workflow.csv` 记录“已通知”状态，避免重复推送\n+- 支持静态白名单（配置）与动态白名单（订阅指令写入 `subscriptions.json`）\n+\n+## 安装\n+\n+将本目录放到 AstrBot 的插件目录中（示例）：\n+\n+- `data/plugins/steam_free_game/`\n+\n+启动 AstrBot 后在控制台为该插件填写配置。\n+\n+## 配置\n+\n+主要配置项在 `_conf_schema.json` 中声明，AstrBot 会在控制台生成配置界面。\n+\n+- `targets_text`: 静态白名单（每行一个 `unified_msg_origin`）\n+- `check_interval_seconds`: 自动检查间隔\n+- `workflow_path_mode`: `workflow.csv` 存放在插件目录或 `data/plugin_data/...`\n+\n+## 指令\n+\n+- `/steamfree_check`：手动检查一次（会走同样的去重逻辑）\n+- `/steamfree_sub`：将当前会话加入推送白名单（写入 `subscriptions.json`）\n+- `/steamfree_unsub`：将当前会话移出推送白名单\n+- `/steamfree_status`：查看当前状态\n+\n+## 文件说明\n+\n+- `workflow.csv`：工作流/去重状态表（动态更新）\n+- `subscriptions.json`：动态白名单（由订阅指令维护，位于 `data/plugin_data/<plugin>/`）\n+\n*** End Patch"}]}assistant to=functions.apply_patch  大发快三有json?>
